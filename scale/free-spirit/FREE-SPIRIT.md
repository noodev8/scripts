# FREE-SPIRIT — segment rules

Overrides to the defaults in `../SEGMENT_REPORTS.md`. Anything not listed here follows the default.

## Override

- **Investigation segment, very small numbers.** Use absolute units, not YoY (no history to compare against).
- **Promote-or-cull check at 90 days** — does this earn a place as a real brick, or get dropped?

Everything else follows the default template.
