# RIEKER-SUM — segment rules

Overrides to the defaults in `../SEGMENT_REPORTS.md`. Anything not listed here follows the default.

## Override

- **No YoY comparison.** RIEKER-SUM has no comparable history (didn't sell last year), so the default last-14d-vs-same-14d-last-year column is meaningless. Compare against the current 12-month baseline only.
- Pace family: Rieker Summer, Mar–Sep, ×210.

Everything else (window definitions, per-SKU drill, notes) follows the default template.
